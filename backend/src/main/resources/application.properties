spring.application.name=backend

# PostgreSQL connection
spring.datasource.url=jdbc:postgresql://${DB_HOST:db}:5432/${DB_NAME:glossaar_dev}
spring.datasource.username=${DB_USER:glossaar}
spring.datasource.password=${DB_PASSWORD:glossaar_dev_password}